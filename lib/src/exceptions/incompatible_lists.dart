import 'package:exception_templates/exception_templates.dart';

/// Error type indicating that an expression involving list arguments
/// could not be calculated because the list length does not match.
class IncompatibleLists extends ErrorType {}
